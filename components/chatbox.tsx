interface ChatboxProps {
  cid: string;
  sid: string;
}

export default function Chatbox({cid, sid}: ChatboxProps) {
  return (
    <div>
      {/* Chatbox {cid} {sid} */}
      Coming soon perhaps
    </div>
  )
}